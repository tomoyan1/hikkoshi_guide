<div class="task-cards">
  <% @tasks.each do |task| %>
    <% if task.task_type == 0 && task.status_id != 3 %> 
      <%= render partial: "task_card", locals: { task: task } %>
    <% end %>
   <% end %>
</div>