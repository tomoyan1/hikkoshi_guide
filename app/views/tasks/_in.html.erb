<div class="task-cards">
  <% @tasks.each do |task| %>
    <% if task.task_type == 1 && task.status_id != 3 %> <%# 新居のタスク %>
      
      <%= render partial: "task_card", locals: { task: task } %>
      
    <% end %>
  <% end %>
</div>